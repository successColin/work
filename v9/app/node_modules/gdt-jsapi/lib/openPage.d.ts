export interface RequestOpenPageParams {
    name: 'profile';
    params?: {
        corpId?: string;
        id?: string;
    };
}
declare function openPage(params: RequestOpenPageParams): Promise<{}>;
declare namespace openPage {
    var version: {
        android: string;
        ios: string;
    };
}
export default openPage;
