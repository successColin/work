export interface StopListenNetworkStatusParams {
    requestId: string;
}
declare function stopListenNetworkStatus(params: StopListenNetworkStatusParams): Promise<string | null>;
declare namespace stopListenNetworkStatus {
    var version: {
        android: string;
        ios: string;
    };
}
export default stopListenNetworkStatus;
