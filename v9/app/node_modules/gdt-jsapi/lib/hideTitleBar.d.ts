export interface HideTitleBarParams {
    onSuccess?: () => void;
    onFail?: () => void;
}
declare function hideTitleBar(): Promise<void>;
declare namespace hideTitleBar {
    var version: {
        android: string;
        ios: string;
    };
}
export default hideTitleBar;
