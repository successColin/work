export interface RecordResult {
    mediaId: string;
    duration: number;
}
declare function onRecordAudioEnd(params: {}): Promise<RecordResult>;
declare namespace onRecordAudioEnd {
    var version: {
        android: string;
        ios: string;
    };
}
export default onRecordAudioEnd;
