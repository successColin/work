declare function playAudio(params: {
    localAudioId: string;
}): Promise<{}>;
declare namespace playAudio {
    var version: {
        android: string;
        ios: string;
    };
}
export default playAudio;
