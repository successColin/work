declare function unsubscribe(params: {
    channel: string;
}): Promise<{}>;
declare namespace unsubscribe {
    var version: {
        android: string;
        ios: string;
    };
}
export default unsubscribe;
