export interface RecordResult {
    mediaId: string;
    duration: number;
}
declare function stopRecordAudio(params: {}): Promise<RecordResult>;
declare namespace stopRecordAudio {
    var version: {
        android: string;
        ios: string;
    };
}
export default stopRecordAudio;
