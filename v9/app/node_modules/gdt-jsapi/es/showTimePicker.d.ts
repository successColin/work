export interface showTimePickerProps {
    format: string;
    value: string;
}
declare function showTimePicker(args: showTimePickerProps): Promise<{
    value: string;
}>;
declare namespace showTimePicker {
    var version: {
        android: string;
        ios: string;
    };
}
export default showTimePicker;
