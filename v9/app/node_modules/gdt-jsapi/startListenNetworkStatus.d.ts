import { OnFailCallback } from './invoker';
import { NETWORK_TYPE } from './getNetworkType';
export interface NetworkStatusResult {
    isConnect: boolean;
    result: NETWORK_TYPE;
}
export interface StartListenNetworkStatusParams {
    onSuccess: (result: NetworkStatusResult) => void;
    onFail: OnFailCallback;
}
declare function startListenNetworkStatus(args: StartListenNetworkStatusParams): Promise<NetworkStatusResult>;
declare namespace startListenNetworkStatus {
    var version: {
        android: string;
        ios: string;
    };
}
export default startListenNetworkStatus;
