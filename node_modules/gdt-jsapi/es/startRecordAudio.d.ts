declare function startRecordAudio(params: {
    maxDuration?: number;
}): Promise<{}>;
declare namespace startRecordAudio {
    var version: {
        android: string;
        ios: string;
    };
}
export default startRecordAudio;
