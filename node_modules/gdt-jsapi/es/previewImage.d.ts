export interface PreviewImageParams {
    urls: string[];
    current?: string;
}
declare function previewImage(args: PreviewImageParams): Promise<{}>;
declare namespace previewImage {
    var version: {
        android: string;
        ios: string;
        pc: string;
    };
}
export default previewImage;
