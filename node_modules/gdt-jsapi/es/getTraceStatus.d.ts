export interface TraceReportParams {
    traceId: string;
}
export interface TraceStatus {
    status: 0 | 1;
}
declare function getTraceStatus(params: TraceStatus): Promise<TraceStatus>;
declare namespace getTraceStatus {
    var version: {
        android: string;
        ios: string;
    };
}
export default getTraceStatus;
