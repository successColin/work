export interface SetNavIconParams {
    showIcon?: boolean;
    iconIndex?: number;
}
declare function setNavIcon(params: SetNavIconParams): Promise<{}>;
declare namespace setNavIcon {
    var version: {
        android: string;
        ios: string;
    };
}
export default setNavIcon;
