export interface ScanParams {
    type?: 'all' | 'qrCode' | 'barCode';
}
declare function scan(params: ScanParams): Promise<{
    text: string;
}>;
declare namespace scan {
    var version: {
        android: string;
        ios: string;
    };
}
export default scan;
