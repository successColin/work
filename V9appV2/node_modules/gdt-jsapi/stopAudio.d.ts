declare function stopAudio(params: {
    localAudioId: string;
}): Promise<{}>;
declare namespace stopAudio {
    var version: {
        android: string;
        ios: string;
    };
}
export default stopAudio;
