export declare type AlertType = 1 | 2;
export interface CreateDingParams {
    users: string[];
    corpId?: string;
    alertType: AlertType;
    text: string;
}
declare function createDing(params: CreateDingParams): Promise<{
    success: boolean;
}>;
declare namespace createDing {
    var version: {
        android: string;
        ios: string;
        pc: string;
    };
}
export default createDing;
