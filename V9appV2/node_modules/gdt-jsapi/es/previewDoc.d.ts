export interface RequestPreviewDocParams {
    fileId?: string;
    fileName?: string;
    fileUrl?: string;
    readCache?: boolean;
}
declare function previewDoc(args: RequestPreviewDocParams): Promise<{}>;
declare namespace previewDoc {
    var version: {
        android: string;
        ios: string;
    };
}
export default previewDoc;
