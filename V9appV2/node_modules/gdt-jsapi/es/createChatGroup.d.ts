export interface createChatGroupParams {
    corpId?: string;
    users?: string[];
}
declare function createChatGroup(params: createChatGroupParams): Promise<{
    id: string;
}>;
declare namespace createChatGroup {
    var version: {
        android: string;
        ios: string;
        pc: string;
    };
}
export default createChatGroup;
